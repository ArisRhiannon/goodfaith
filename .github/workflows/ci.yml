name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: set up the toolchain, then run lint + test + build for goodfaith
      - run: echo "Configure CI steps for goodfaith"
